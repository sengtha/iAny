{
  "name": "@iany/core",
  "version": "0.1.0",
  "private": true,
  "description": "iAny shared brain — platform-agnostic RAG core (types, chunking, retrieval orchestration, prompts, TTS text, pack format). Consumed by the PWA (Vite) and the mobile app (Expo/Metro) so both behave identically and knowledge packs stay portable.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
